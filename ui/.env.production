# Production build calls the API same-origin: host nginx serves the SPA and
# reverse-proxies /auth, /conversations, /events, /files, /documents, /health
# to the backend. Empty value => client.ts API_BASE_URL = "" (relative URLs).
VITE_API_BASE_URL=